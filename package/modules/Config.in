menu "Kernel Modules"
	depends on BR2_LINUX_KERNEL

config BR2_PACKAGE_MODULES
	bool
	depends on BR2_LINUX_KERNEL

config BR2_PACKAGE_MODULES_HELLO
	bool "hello"
	select BR2_PACKAGE_MODULES
	help
	  Dummy module.

endmenu

comment "Kernel modules require a Linux kernel"
	depends on !BR2_LINUX_KERNEL
